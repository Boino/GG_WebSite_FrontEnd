# Remaining Fidelity TODO

1. Replace placeholder graphics with licensed original-like custom assets created specifically for this clone.
2. Add a quick-view modal on collection pages to mirror Squarespace quick-view interactions more closely.
3. Add richer product image galleries per product with selectable thumbnails and zoom behavior.
4. Add keyboard-trap + focus-return behavior for mobile menu to match stricter accessibility parity.
5. Add Playwright visual regression snapshots against the live site for desktop/mobile breakpoints.
6. Add unit/integration tests for cart reducer logic, form validation, and route-level rendering.
7. Add optional mock API layer (`/api/cart`, `/api/contact`) to separate UI from storage behavior.
8. Tune typography and spacing against captured screenshots for closer pixel-level parity.
